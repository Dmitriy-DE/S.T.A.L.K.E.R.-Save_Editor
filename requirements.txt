# Runtime dependencies of the editor.
#
# The parser and storage layer stay on the Python standard library; these two
# are the desktop UI and the native Kraken decoder.
PySide6==6.11.2
# Native decoder wheel for Linux/Windows x86_64. Linux checkouts can also use
# the bundled vendor/ooz.abi3.so, which is the same binary from the 0.0.8 wheel
# and ships under GPL-3.0-compatible terms; see THIRD_PARTY_NOTICES.md.
pyooz==0.0.8
