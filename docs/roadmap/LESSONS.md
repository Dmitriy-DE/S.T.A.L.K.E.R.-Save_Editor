# Уроки: где ошибались и что из этого следует (на 2026-09-26)

Живой документ. Каждая запись: что случилось → почему → правило. Правила
переносятся в C#-порт (ARCHITECTURE.md нового репо) и в AGENTS.md.

## Ошибки в данных и форматах

| Что случилось | Почему | Правило |
|---|---|---|
| Запись в облако S2 через `FileWrite` «успешна», но игра файла не видит; рядом появлялись двойники | `FileWrite` пишет в корень по умолчанию, а S2 хранит сейвы в Auto-Cloud `WinAppDataLocal`; статус «persisted» проверял только локальное состояние | Облако S2 = локальная папка игры + сеанс «игры» Steam; успех — только если Steam web отдаёт те же байты (SC-1) |
| Двойники в корне облака сломали список (видно 2 файла вместо 50) | Список брался из RemoteStorage, пока он не пуст; после экспериментов он перестал быть пустым | Для игр с Auto-Cloud список всегда из папки игры (web), не из RemoteStorage |
| Добавленная в ЧН экза надевалась вместо костюма и несла чужие апгрейды | Новый предмет — копия существующей записи; шаблоном оказался надетый костюм, копия унаследовала «слот» и апгрейды | Копия шаблона всегда получает «рюкзак» и пустые апгрейды; тест на надетый шаблон (#187) |
| В ЧН место предмета не читалось | ЧН хранит место одним байтом (1 слот, 2 пояс, 3 рюкзак), а код знал только 16-битный формат ЗП | Формат поля проверять по реальным сейвам каждой игры, не переносить из соседней |
| Первый вывод «игра не создаёт новых записей в S2» оказался неверным | Сравнивал только множества номеров; номера переиспользуются | Сравнивать содержимое записи по номеру в обоих сейвах, а не только номера |
| Сейвы EE считались «несовместимыми» | Опирались на FAQ, не на байты | Сначала разобрать реальный файл: контейнер EE оказался оригинальным |

## Ошибки процесса

| Что случилось | Почему | Правило |
|---|---|---|
| OTA на Linux «не работала» после исправления | Исправление было в 0.7.3, а обновлял установленный 0.7.2 со старым кодом | Проверять, какая версия выполняет обновление; фикс апдейтера работает только со следующего релиза |
| Фоновые ожидания висели сутки | `gh pr checks --json` не поддерживается этим `gh`; цикл ждал вечно | У каждого ожидания лимит времени; проверять команду один раз перед циклом |
| Очереди слияний ждали сами себя | `pgrep -f` находил текст собственной команды | Одна очередь; ожидание по ID задачи, не по тексту команды |
| Методы вставились в чужой класс | Текстовая замена по первому совпадению в файле с двумя похожими классами | После скриптовой правки — mypy/ruff до коммита |
| При слиянии переводов стёрты строки веб-версии | Удалял «лишние» ключи по списку desktop, а у веба свои строки | Удалять только то, что сама проверка назвала устаревшим |
| Строка перевода `"{0}: {1}"` ломала распознавание ошибок | Шаблон без слов совпадает с любым текстом | Не переводить строки без текста; тест обратного сопоставления |
| Бриф для Gemini был готов, но не отдан | Не сказал владельцу | Каждый бриф сразу отправлять файлом и писать, кому отдать |
| Codex писал резервные копии в реальную папку пользователя (`codex-check-inventory…` в backups) | Проверка запускалась без изолированного HOME | Проверки Codex — только с временным HOME |
| «Пессимизм»: предлагал установить S2, хотя места нет; сомневался в том, что уже работало | Не учёл ограничения владельца | Если владелец говорит «работало» — искать, что упущено в проверке |

## Что работает хорошо

- Пары сейвов «до/после» от владельца + байтовое сравнение дают формат за
  часы (тайник S2, место в ЧН, EE).
- L5 через GFN: один сейв с узнаваемыми числами (50/40/300/400).
- Проверка в памяти на реальных сейвах перед тестами на синтетике.
