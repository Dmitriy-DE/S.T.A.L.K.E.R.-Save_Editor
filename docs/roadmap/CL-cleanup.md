# CL — Чистка

## CL-1 — Локальные папки

**Кто:** Claude · **Размер:** S

**Удалить** (это сборочные выходы, их можно воссоздать):

- `dist/` (770 МБ);
- `release-input/` и `release-output/` (по 303 МБ);
- `__pycache__`;
- временные `*.log` в корне, если есть.

**`artifacts/` (29 МБ):**

- `ui-review/` и `references/` — сначала проверить, используются ли тестами или документацией;
- лишнее удалить, остальное перенести в `docs/`.

**Не трогать без явного «да» владельца:**

- `fixture_*` бэкапы;
- `~/.local/share/Stalker2SaveEditor/{backups,private-import-*}`;
- сейвы в папках игр.

**Проверка:** `.gitignore` покрывает всё удалённое; `git status` чистый после сборки.

## CL-2 — Ветки Git

**Кто:** Claude · **Размер:** S

**Шаги.**

1. Удалить локальные и удалённые ветки, уже слитые в `main`: `docs/v0.7.*-release`, `feat/*`, `fix/*` и т.д. Список — в PR или отчёте.
2. `codex/ui-canonical-rebuild`, `review/core-audit`, `docs/release-v0.6.0`, `fix/deb-font-override`: проверить, слиты ли. Если не слиты — показать владельцу, что в них, и удалять только с «да».
3. `gh-pages` оставить, если используется для web; иначе спросить.

## CL-3 — Документация

**Кто:** Claude (STATUS, README) + Codex (архив и ссылки) · **Размер:** M · **Зависит:** ST-9

**Шаги.**

1. `docs/STATUS.md` (1010 строк) → короткий актуальный статус на 1–2 экрана: capability-таблица из RL-4, известные ограничения, ссылка на историю.
   - История версий → `docs/history/CHANGELOG.md`.
2. Старая очередь `docs/tasks/*` и `docs/plans/*` → `docs/history/queue-0.5/`. Живой план — `docs/roadmap/`.
3. Заменить старые ссылки `S.T.A.L.K.E.R.-2-HoC---Save_Editor` → `S.T.A.L.K.E.R.-Save_Editor` по всему репо. GitHub редиректит, но это мусор.
4. `docs/README.md`: карта документации.
5. Проверка ссылок: скрипт или `lychee`, если доступен.

## CL-4 — Мёртвый код

**Кто:** Codex · **Размер:** M · **Уровень:** L2

**Шаги.**

1. `vulture` (min-confidence 80) и `coverage run -m pytest` → кандидаты: функции и модули без вызовов и без покрытия.
2. Проверенные кандидаты:
   - `editor/s2_presentation.py` — тонкая обёртка над `s2_names`;
   - `editor/equipment_research.py`;
   - `tools/*` без упоминаний в Makefile, docs и тестах.
3. Удалять по одному логическому блоку на коммит. Тесты зелёные после каждого.
4. **Не удалять:** всё, что упоминается в `docs/evidence/*`, `tools/prepare_ingame_verification.py`, `tools/verify_*`.

**Результат аудита 2026-09-25:** `vulture --min-confidence 80` по Python-файлам репозитория не нашёл неиспользуемых функций или модулей; два предупреждения относятся к параметрам pytest hook/callback. `editor/s2_presentation.py` не имел desktop/web runtime callers и удалён отдельным логическим коммитом; проверка имён теперь вызывает `s2_readable_name` напрямую, а web bundle пересобран без wrapper. `editor/equipment_research.py` покрыт тестами на 90% и упоминается в `docs/evidence/`, поэтому сохранён. Единственный `tools/*.py` без прямой ссылки в Makefile, документации или тестах — `tools/build_chrome_pack.py`; текущий UI его текстуры не загружает, однако дизайн-спецификация [Zone launcher](../superpowers/specs/2026-09-18-zone-launcher-and-save-library-design.md) планирует использовать этот pack для game-skinned shell, поэтому генератор и ассеты сохранены как запланированный ресурс. Другие подтверждённые блоки для удаления не найдены.

## CL-5 — Профилирование и оптимизация

**Кто:** Claude · **Размер:** M · **Зависит:** RL-5 · **Уровень:** L3

**Целевые метрики на машине владельца, из `.deb`:**

| Сценарий | Цель |
|---|---|
| Холодный старт | < 2 с |
| Открытие S2 27 МБ | < 3 с |
| Открытие ЗП | < 1 с |
| Прокрутка инвентаря на 200 предметов | без просадок |

Метод: `cProfile` и `py-spy` (если есть) → 3 главных узких места → исправления → замер до и после.
