# R02 — Каталог SID отдельно от доказанного save mapping

GitHub: [R02](https://github.com/Dmitriy-DE/S.T.A.L.K.E.R.-2-HoC---Save_Editor/issues/19).
Статус: waiting_dependencies. Приоритет: P2. Тип gate: research.

**Зависимости:** [R01](R01.md).
**Исполнитель:** GPT-5.6 Luna, одна карточка за проход.
**Спецификация:** [CROSS_PLATFORM_EDITOR](../specs/CROSS_PLATFORM_EDITOR.md).

## Границы изменения

- `data/item_catalog.json`
- `docs/research/PROTOTYPE_MAPPING.md`
- `tools/validate_catalog.py`
- `tests/test_catalog.py`
- `ui/inventory_model.py`

Это planned paths: создаются в этой задаче или её зависимостях; команды ниже не являются уже существующим/пройденным test suite. Не менять соседние подсистемы ради cleanup. Если обнаружена необходимость изменения контракта, сначала описать его в карточке и spec, не молча переименовывать API.

## Вход и выход

Catalog entry: sid, display_name, category, source_url, source_revision. Mapping entry отдельно: save format/game build, parse rule, evidence IDs, confidence. Не использовать type-key как SID без доказательства.

## Шаги

- [ ] Прочитать AGENTS.md, STATUS и эту карточку; проверить accepted evidence/commits зависимостей. Создать `task/r02` от актуального main; не rebase/force push чужую работу.
- [ ] Импортировать небольшой проверенный набор public SID с источниками; лицензии данных и изображений учитывать отдельно.
- [ ] Исследовать >=3 контролируемых примера и competing encodings. Negative examples обязательны: один type-key не даёт universal mapping.
- [ ] Validator запрещает confirmed mapping без evidence IDs и применимости. Unknown сохраняется как unknown.
- [ ] Подключить human names только для confirmed entries; если mapping не найден, завершить каталог и зафиксировать blocker mapping, Add остаётся disabled.
- [ ] Для regression сначала показать падающий тест на исходном поведении, затем минимальную правку и PASS. Для research сначала проверить входные SHA и контролируемость эксперимента.
- [ ] Выполнить перечисленные проверки, затем `python -m pytest tests` для доступного suite и `make check`; записать exit codes. GUI tests требуют requirements-ui, вводимых U02.
- [ ] Обновить фактический статус карточки, приложить evidence и ограничения, создать один PR. Не брать следующую карточку автоматически и не мержить самостоятельно.

## Приёмка

- python -m pytest tests/test_catalog.py -v
- Catalog validation PASS; mapping gate PASS только при evidence, отсутствие evidence не подменяется public SID list.

## Stop conditions

Недоступные личные данные, непрошедший dependency gate или неизвестный формат не заменять догадкой. Записать BLOCKED и конкретные missing inputs. Для research gate отчёт с неопровергнутыми кандидатами не открывает зависимую mutating-задачу. Тестирование реального cloud/game выполняется только по отдельному поручению владельца.
