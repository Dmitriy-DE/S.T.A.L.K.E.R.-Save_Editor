# R06 — Клонирование одного подтверждённого типа предмета

GitHub: [R06](https://github.com/Dmitriy-DE/S.T.A.L.K.E.R.-2-HoC---Save_Editor/issues/23).
Статус: waiting_dependencies. Приоритет: P2. Тип gate: evidence.

**Зависимости:** [R05](R05.md), [U04](U04.md).
**Исполнитель:** GPT-5.6 Luna, одна карточка за проход.
**Спецификация:** [CROSS_PLATFORM_EDITOR](../specs/CROSS_PLATFORM_EDITOR.md).

## Границы изменения

- `editor/object_mutations.py`
- `save_format.py`
- `tests/test_clone_item.py`
- `docs/research/CLONE_ACCEPTANCE.md`

Это planned paths: создаются в этой задаче или её зависимостях; команды ниже не являются уже существующим/пройденным test suite. Не менять соседние подсистемы ради cleanup. Если обнаружена необходимость изменения контракта, сначала описать его в карточке и spec, не молча переименовывать API.

## Вход и выход

Первый clone ограничен классом, подтверждённым R05; новый handle и все self/owner/registry refs по принятой схеме. Никакого копирования guessed record window.

## Шаги

- [ ] Прочитать AGENTS.md, STATUS и эту карточку; проверить accepted evidence/commits зависимостей. Создать `task/r06` от актуального main; не rebase/force push чужую работу.
- [ ] Проверить registry gate и сформулировать exact typed operation по схеме R05; missing schema = blocked.
- [ ] Тесты duplicate handle rejection, allocator exhaustion, missing prototype, occupied footprint и unsupported components.
- [ ] Добавить создание нового instance с registry/count/owned/grid updates как одну транзакцию; исходный item неизменен.
- [ ] Контроль game load/re-save по поручению; повторно разобрать обе независимые identity.
- [ ] Для regression сначала показать падающий тест на исходном поведении, затем минимальную правку и PASS. Для research сначала проверить входные SHA и контролируемость эксперимента.
- [ ] Выполнить перечисленные проверки, затем `python -m pytest tests` для доступного suite и `make check`; записать exit codes. GUI tests требуют requirements-ui, вводимых U02.
- [ ] Обновить фактический статус карточки, приложить evidence и ограничения, создать один PR. Не брать следующую карточку автоматически и не мержить самостоятельно.

## Приёмка

- python -m pytest tests/test_clone_item.py -v
- Ровно один новый объект, уникальный handle, references resolved, исходный объект неизменен, game evidence.

## Stop conditions

Недоступные личные данные, непрошедший dependency gate или неизвестный формат не заменять догадкой. Записать BLOCKED и конкретные missing inputs. Для research gate отчёт с неопровергнутыми кандидатами не открывает зависимую mutating-задачу. Тестирование реального cloud/game выполняется только по отдельному поручению владельца.
