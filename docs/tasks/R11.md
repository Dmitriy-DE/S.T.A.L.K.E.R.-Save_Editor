# R11 — Компактная пересборка Kraken с безопасным fallback

GitHub: [R11](https://github.com/Dmitriy-DE/S.T.A.L.K.E.R.-2-HoC---Save_Editor/issues/28).
Статус: in_review. Приоритет: P3. Тип gate: research.

**Зависимости:** [S01](S01.md), [P01](P01.md).
**Исполнитель:** GPT-5.6 Luna, одна карточка за проход.
**Спецификация:** [CROSS_PLATFORM_EDITOR](../specs/CROSS_PLATFORM_EDITOR.md).

## Границы изменения

- `editor/kraken_blocks.py`
- `save_format.py`
- `tests/test_compact_rebuild.py`
- `docs/research/COMPACT_REBUILD.md`
- `tools/build_web_bundle.py` и сгенерированный `web/pysrc.json` — синхронизация
  общего ядра для web-поставки.

Это planned paths: создаются в этой задаче или её зависимостях; команды ниже не являются уже существующим/пройденным test suite. Не менять соседние подсистемы ради cleanup. Если обнаружена необходимость изменения контракта, сначала описать его в карточке и spec, не молча переименовывать API.

## Вход и выход

Новый optional rebuild mode сохраняет исходные compressed blocks только при доказанных block boundaries/dependencies. Всегда доступен текущий deterministic full rebuild.

## Шаги

- [x] Прочитать AGENTS.md, STATUS и эту карточку; проверить accepted evidence/commits зависимостей. Создать `codex/r11-compact-rebuild` от актуального main; не rebase/force push чужую работу.
- [x] Исследовать restart/dependency boundaries на compressed corpus; нельзя заменять один блок, если последующие зависят от старого dictionary.
- [x] Тесты cross-block changes, raw length changes, last partial block, unsupported stream и no-op byte identity.
- [x] При неоднозначности full rebuild fallback с явным mode/size. Каждый результат full decompress==expected raw и CRC.
- [x] Сравнить output size/time на corpus; game load/re-save оставлен отдельным ручным gate, поскольку на текущем компьютере нет установки S.T.A.L.K.E.R. 2 и владелец запретил новый запуск игры.
- [x] Для regression сначала показать падающий тест на исходном поведении, затем минимальную правку и PASS. Для research проверены входные CRC/размеры и контролируемость read-only эксперимента.
- [x] Выполнить полный `python -m pytest tests` (303 passed, exit 0) и `make check` (exit 0). GUI tests прошли в доступном Qt environment.
- [x] Обновить фактический статус, приложить evidence и ограничения, создать один PR (#74). Не брать следующую карточку автоматически и не мержить самостоятельно.

## Приёмка

- python -m pytest tests/test_compact_rebuild.py -v
- Unsupported stream → valid full rebuild; compact/raw bytes identical after decompress; game acceptance отдельно.

## Stop conditions

Недоступные личные данные, непрошедший dependency gate или неизвестный формат не заменять догадкой. Записать BLOCKED и конкретные missing inputs. Для research gate отчёт с неопровергнутыми кандидатами не открывает зависимую mutating-задачу. Тестирование реального cloud/game выполняется только по отдельному поручению владельца.
