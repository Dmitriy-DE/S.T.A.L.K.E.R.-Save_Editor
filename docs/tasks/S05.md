# S05 — Steam worker: restart, timeout и единственный reader

GitHub: [S05](https://github.com/Dmitriy-DE/S.T.A.L.K.E.R.-2-HoC---Save_Editor/issues/5).
Статус: waiting_dependencies. Приоритет: P0. Тип gate: code.

**Зависимости:** [S01](S01.md).
**Исполнитель:** GPT-5.6 Luna, одна карточка за проход.
**Спецификация:** [CROSS_PLATFORM_EDITOR](../specs/CROSS_PLATFORM_EDITOR.md).

## Границы изменения

- `steam_cloud.py`
- `tests/fake_worker.py`
- `tests/test_worker_lifecycle.py`

Это planned paths: создаются в этой задаче или её зависимостях; команды ниже не являются уже существующим/пройденным test suite. Не менять соседние подсистемы ради cleanup. Если обнаружена необходимость изменения контракта, сначала описать его в карточке и spec, не молча переименовывать API.

## Вход и выход

Сохранить публичные SteamWorker start/connect/request/read_file/write_file/close. После timeout текущий процесс считается непригодным; следующий явный connect создаёт новую сессию.

## Шаги

- [ ] Прочитать AGENTS.md, STATUS и эту карточку; проверить accepted evidence/commits зависимостей. Создать `task/s05` от актуального main; не rebase/force push чужую работу.
- [ ] Создать fake helper с тем же line-delimited JSON; режимы crash, delayed reply, malformed JSON, stderr flood, chunked response.
- [ ] Regression: request на умершем процессе не deadlock. Regression: delayed ответ после timeout не становится ответом следующего запроса.
- [ ] Разделить start и locked request так, чтобы не было recursive Lock; использовать один управляемый reader и drain stderr; timeout закрывает pipes и reap process.
- [ ] Убрать бесконечные blocking read/write paths, ограничить response size разумным documented пределом для 27 MB save. Не логировать byte arrays.
- [ ] Для regression сначала показать падающий тест на исходном поведении, затем минимальную правку и PASS. Для research сначала проверить входные SHA и контролируемость эксперимента.
- [ ] Выполнить перечисленные проверки, затем `python -m pytest tests` для доступного suite и `make check`; записать exit codes. GUI tests требуют requirements-ui, вводимых U02.
- [ ] Обновить фактический статус карточки, приложить evidence и ограничения, создать один PR. Не брать следующую карточку автоматически и не мержить самостоятельно.

## Приёмка

- python -m pytest tests/test_worker_lifecycle.py -v
- Каждый timeout/crash test имеет внешний time bound; нет orphan process/thread, нет real Steam calls.

## Stop conditions

Недоступные личные данные, непрошедший dependency gate или неизвестный формат не заменять догадкой. Записать BLOCKED и конкретные missing inputs. Для research gate отчёт с неопровергнутыми кандидатами не открывает зависимую mutating-задачу. Тестирование реального cloud/game выполняется только по отдельному поручению владельца.
