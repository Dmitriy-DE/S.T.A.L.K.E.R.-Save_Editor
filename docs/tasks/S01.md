# S01 — Переносимые fixtures и baseline-тесты

GitHub: [S01](https://github.com/Dmitriy-DE/S.T.A.L.K.E.R.-2-HoC---Save_Editor/issues/1).
Статус: ready. Приоритет: P0. Тип gate: code.

**Зависимости:** нет — первая готовая задача.
**Исполнитель:** GPT-5.6 Luna, одна карточка за проход.
**Спецификация:** [CROSS_PLATFORM_EDITOR](../specs/CROSS_PLATFORM_EDITOR.md).

## Границы изменения

- `tests/conftest.py`
- `tests/test_save_baseline.py`
- `requirements-dev.txt`
- `Makefile`

Это planned paths: создаются в этой задаче или её зависимостях; команды ниже не являются уже существующим/пройденным test suite. Не менять соседние подсистемы ради cleanup. Если обнаружена необходимость изменения контракта, сначала описать его в карточке и spec, не молча переименовывать API.

## Вход и выход

tests/conftest.py: synthetic_save() -> bytes создаёт искусственный контейнер с MONEY_ANCHOR, 4 owned handles 0x30000001..04, двумя grid cells и однозначными object records. Никаких байтов личного сейва. Fixture не доказывает реальность глобальной структуры игры.

## Шаги

- [ ] Прочитать AGENTS.md, STATUS и эту карточку; проверить accepted evidence/commits зависимостей. Создать `task/s01` от актуального main; не rebase/force push чужую работу.
- [ ] Сохранить текущий selftest_real.py как отдельный private-corpus test; прочитать save_format.py:218 и :286 до создания fixture.
- [ ] Создать synthetic_save с money=100, owned_flag=1, четырьмя handles, grid handles 0x30000001/02 в (0,0)/(1,0), stack handle 0x30000001 count=2 weight=0.2 kind=4. Record size >=36, marker +18=0x38; x/y совпадают с grid, у остальных handles FFFF/FFFF. Файл собрать через rebuild_uncompressed.
- [ ] Добавить тесты чтения, денег 100→900000, count 2→3 с весом 0.3, bad CRC, missing/duplicate anchor; проверить неизменность остальных raw bytes для money/stack.
- [ ] Зафиксировать pytest в requirements-dev.txt; make test запускает python -m pytest tests. Не менять runtime ради удобства fixture.
- [ ] Для regression сначала показать падающий тест на исходном поведении, затем минимальную правку и PASS. Для research сначала проверить входные SHA и контролируемость эксперимента.
- [ ] Выполнить перечисленные проверки, затем `python -m pytest tests` для доступного suite и `make check`; записать exit codes. GUI tests требуют requirements-ui, вводимых U02.
- [ ] Обновить фактический статус карточки, приложить evidence и ограничения, создать один PR. Не брать следующую карточку автоматически и не мержить самостоятельно.

## Приёмка

- python -m pytest tests/test_save_baseline.py -v
- Ожидается: минимум 7 сценариев PASS; corruption/ambiguous input отвергнуты, правильные данные читаются.
- make selftest SAVE=/absolute/path/to/original-D639.sav — отдельно только при наличии private corpus.

## Stop conditions

Недоступные личные данные, непрошедший dependency gate или неизвестный формат не заменять догадкой. Записать BLOCKED и конкретные missing inputs. Для research gate отчёт с неопровергнутыми кандидатами не открывает зависимую mutating-задачу. Тестирование реального cloud/game выполняется только по отдельному поручению владельца.

## Стартовый код fixture и проверок

Это код для реализации в S01, ещё не добавленный в test suite. Структура синтетическая; исходные игровые байты не используются.

```python
# tests/conftest.py
import struct
import pytest
import save_format as sf

@pytest.fixture
def synthetic_save():
    handles = (0x30000001, 0x30000002, 0x30000003, 0x30000004)
    raw = bytearray(b"SYNTHETIC-FIXTURE\x00" + sf.MONEY_ANCHOR)
    raw += struct.pack("<IIH", 100, 1, len(handles))
    raw += struct.pack("<4I", *handles)
    raw += struct.pack("<H", 2)
    raw += struct.pack("<IHH", handles[0], 0, 0)
    raw += struct.pack("<IHH", handles[1], 1, 0)
    raw += bytes(64)
    for index, handle in enumerate(handles):
        record = bytearray(64)
        struct.pack_into("<I", record, 0, handle)
        struct.pack_into("<HH", record, 11,
                         index if index < 2 else 65535,
                         0 if index < 2 else 65535)
        record[18] = 0x38
        struct.pack_into("<I", record, 19, 2)
        struct.pack_into("<f", record, 24, 0.2)
        record[31] = 4
        raw += record
    return sf.rebuild_uncompressed(bytes(raw))
```

```python
# tests/test_save_baseline.py — старт, дополнить случаями из карточки
import pytest
import save_format as sf

def test_money_changes_only_wallet(synthetic_save):
    before = sf.decompress_save(synthetic_save)
    offset, _ = sf.locate_money(before)
    result = sf.patch_save(synthetic_save, new_money=900000)
    after = sf.decompress_save(result.data)
    assert sf.locate_money(after)[1] == 900000
    assert before[:offset] == after[:offset]
    assert before[offset + 4:] == after[offset + 4:]

def test_bad_crc_rejected(synthetic_save):
    broken = bytearray(synthetic_save)
    broken[-1] ^= 1
    with pytest.raises(sf.SaveError, match="CRC"):
        sf.patch_save(bytes(broken), new_money=900000)
```
