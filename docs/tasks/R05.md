# R05 — Реестр объектов, allocator и prototype references

GitHub: [R05](https://github.com/Dmitriy-DE/S.T.A.L.K.E.R.-2-HoC---Save_Editor/issues/22).
Статус: waiting_dependencies. Приоритет: P2. Тип gate: research.

**Зависимости:** [R01](R01.md), [R02](R02.md).
**Исполнитель:** GPT-5.6 Luna, одна карточка за проход.
**Спецификация:** [CROSS_PLATFORM_EDITOR](../specs/CROSS_PLATFORM_EDITOR.md).

## Границы изменения

- `docs/research/OBJECT_REGISTRY.md`
- `tools/analyze_registry.py`
- `tests/test_registry_analysis.py`

Это planned paths: создаются в этой задаче или её зависимостях; команды ниже не являются уже существующим/пройденным test suite. Не менять соседние подсистемы ради cleanup. Если обнаружена необходимость изменения контракта, сначала описать его в карточке и spec, не молча переименовывать API.

## Вход и выход

Read-only analysis с кандидатами registry boundaries/count/allocator/reference graph; результат должен содержать точную схему и ограничения, не next-known-record guess.

## Шаги

- [ ] Прочитать AGENTS.md, STATUS и эту карточку; проверить accepted evidence/commits зависимостей. Создать `task/r05` от актуального main; не rebase/force push чужую работу.
- [ ] Контролируемые acquire/drop/equip или console spawn при доступности локальной игры; >=3 разных SID.
- [ ] Найти новые handles, all references, object count, length encoding, owner/prototype/instance identity. Проверить reuse и persistence after re-save.
- [ ] Исключить false boundaries, offsets за известным window не считать свободным пространством.
- [ ] Документировать allocator algorithm только при доказательствах; иначе указать missing inputs и держать R06/R07/R08 blocked.
- [ ] Для regression сначала показать падающий тест на исходном поведении, затем минимальную правку и PASS. Для research сначала проверить входные SHA и контролируемость эксперимента.
- [ ] Выполнить перечисленные проверки, затем `python -m pytest tests` для доступного suite и `make check`; записать exit codes. GUI tests требуют requirements-ui, вводимых U02.
- [ ] Обновить фактический статус карточки, приложить evidence и ограничения, создать один PR. Не брать следующую карточку автоматически и не мержить самостоятельно.

## Приёмка

- python -m pytest tests/test_registry_analysis.py -v
- Gate: parse whole affected registry, unique identity, references reconciled и controlled A/B/C evidence.

## Stop conditions

Недоступные личные данные, непрошедший dependency gate или неизвестный формат не заменять догадкой. Записать BLOCKED и конкретные missing inputs. Для research gate отчёт с неопровергнутыми кандидатами не открывает зависимую mutating-задачу. Тестирование реального cloud/game выполняется только по отдельному поручению владельца.
