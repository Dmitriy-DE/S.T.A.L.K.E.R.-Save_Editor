# M05 — Ручные пути и их запоминание

**Зависимости:** M04. **Gate:** code. **Priority:** P2.

## Scope

Если автопоиск не нашёл или нашёл не то — пользователь указывает вручную:
корень Steam, папку игры или папку сохранений. Указанное имеет приоритет над
найденным.

Хранить в существующем каталоге пользовательских данных
(`editor/platforms.user_data_dir()`), рядом с настройками. Формат — JSON с
версией схемы. Битый или чужой файл настроек не должен ронять запуск: читать
best-effort и продолжать с пустыми настройками.

## Чего не делать

Не хранить абсолютные пути в репозитории и не коммитить настройки. Не считать
сохранённый путь вечным: если он исчез, сообщить и предложить выбрать заново.

## Проверка

- Ручной путь перекрывает автопоиск.
- Сохранённый путь исчез → понятное сообщение, автопоиск снова доступен.
- Битый JSON настроек → запуск проходит, настройки пустые.
- `make check`, `make test`.

## Результат — 2026-09-15

- Реализовано в коммите `851a30c` на ветке `codex/m05-manual-paths`.
- Создан отдельный [PR #52](https://github.com/Dmitriy-DE/S.T.A.L.K.E.R.-2-HoC---Save_Editor/pull/52).
- Добавлен `editor.settings`: versioned JSON хранится в
  `editor.platforms.user_data_dir()/settings.json`, запись выполняется
  атомарно. Повреждённый, чужой или неподдержанный JSON даёт пустые настройки
  и понятное предупреждение без падения запуска.
- Qt-вкладка «Настройки» позволяет задать корень Steam, папку игры и папку
  сохранений для каждой из четырёх игр. Валидный save/game/Steam root имеет
  приоритет над auto-search; исчезнувший каталог явно показывается и
  возвращает автопоиск, пока пользователь не выберет путь заново.
- Проверки: `PYTHON=.venv/bin/python make check` — exit 0;
  `PYTHON=.venv/bin/python make test` — exit 0, 197 passed.
- Не проверено: реальные Windows/GOG/Proton установки и игровой runtime;
  настройки не содержат игровых байтов и не коммитятся в репозиторий.
