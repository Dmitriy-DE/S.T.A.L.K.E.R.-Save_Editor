# Локальный пакет и web deployment — 2026-09-15

Этот документ фиксирует только проверенный на текущем Linux-хосте результат.
Личные сейвы, их содержимое и каталоги в evidence не записывались.

## Исходник

- branch: `codex/m06-xray-container`
- web bundle source commit: `8b131ff1e431968f51b1c82f6072dcbcb58f8057`
- Linux package source commit: `2dd8692d393acc5501c6ac86d44c314359cb923e`
- build manifest: `source_dirty: false`
- target: Linux x86_64, Python `3.14.4`, PyInstaller `6.22.3`, PySide6 `6.11.2`
- generated web bundle/theme and browser catalog are generated and checked;
  package source tree was clean at build time

## Gates

| Команда | Результат |
|---|---|
| `PYTHON=.venv/bin/python make check` | exit 0 |
| `PYTHON=.venv/bin/python make test` | exit 0, `278 passed` |
| `node --check web/app.js` | exit 0 |
| `git diff --check` | exit 0 |
| `TMPDIR=/home/dmytro/.cache/save-editor-build-tmp PYTHON=.venv/bin/python make package` | exit 0 |
| packaged `SaveEditor-diagnostic --help` | exit 0 |
| packaged `SaveEditor-diagnostic --diagnostic` | exit 0, `decoder: loaded`, Qt `6.11.2` |
| `PYTHON=.venv/bin/python make web-deploy` | exit 0 |

Первая попытка package использовала маленький `/tmp` и получила ошибку
`Disk quota exceeded` внутри `dpkg-deb`; повтор с `TMPDIR` на обычной файловой
системе прошёл. Это ограничение среды, а не пропущенный package gate.

## Linux artifacts

Файлы находятся в локальном, игнорируемом `dist/` и не добавлены в Git.
Проверка выполнена командой `(cd dist && sha256sum -c SHA256SUMS)`.

| Artifact | SHA-256 |
|---|---|
| `SaveEditor-linux-x86_64-v0.4.0.tar.gz` | `3f07270f6854dff3ce20e5b7a14bdcd2ad044d4d5e0eea214d5358eadfb8f3f4` |
| `stalker2-save-editor_0.4.0_amd64.deb` | `671b4d2fefc45a80e8cbf40231b8e6a904d4d987504266bfcbb6251b952905df` |

Portable archive содержит `SaveEditor`, `SaveEditor-diagnostic`,
`BUILD_MANIFEST.json` и `SOURCE_COMMIT.txt`. В bundle decoder загружается из
вложенного `ooz.abi3.so`; Steam helper не вкладывается.

## Cloudflare Pages

Команда deploy опубликовала только статическую `web/`:

- deployment revision: `4d833b6f`
- preview: <https://4d833b6f.stalker2-save-editor.pages.dev>
- canonical URL: <https://stalker2-save-editor.pages.dev>
- canonical и preview ответили `HTTP 200`
- `pysrc.json` read-back совпал с локальным generated source hash
  `8de943c0f86e342137b8e22f2f047c6c60930bc7de499c2010bd069acb5fe654`;
  `catalogs.json` содержит 389/417/434 official metadata entries для
  SoC/CS/CoP
- `app.js`, `pysrc.json` и `catalogs.json` ответили `200` с ожидаемыми
  JavaScript/JSON content types
- в опубликованном bundle присутствуют release/capability markers
  `stalker-soc`, `stalker-cs` и `stalker-cop`; `web/` не содержит `.sav`,
  `.bak`, `.dds` или `.info`

Web остаётся local-file-only: сейв читается в браузере и не отправляется на
сервер. Реальное открытие/редактирование в конкретном браузере и game
load/re-save этим статическим HTTP read-back не объявляются проверенными.

## Live browser smoke — 2026-09-15

Runtime smoke через локальный file picker был выполнен на предыдущем
опубликованном revision `39103a17`, без загрузки сейва на сервер. Новый
revision `4d833b6f` содержит тот же generated runtime source hash
`8de943c0f86e342137b8e22f2f047c6c60930bc7de499c2010bd069acb5fe654`; для него
отдельно выполнен HTTP/read-back gate выше:

- S.T.A.L.K.E.R. 2: реальный контейнер `6.40 MB`, CRC `PASS`, исходный SHA
  `bc7435ca9e22…`; предпросмотр изменения денег `56995 → 900000` создал
  копию `25.75 MB`, исходный файл не изменился.
- Original Call of Pripyat: реальный `.scop`, X-Ray LZO/container `OK`,
  исходный SHA `c87fb75f1546…`; каталог `434` ключей, `wpn_ak74 × 1`
  добавлен через UI, предпросмотр создал копию `4.30 MB`, скачивание показало
  `1_edited.scop`, исходный файл не изменился.

Это browser/runtime smoke, а не доказательство загрузки изменённого файла самой
игрой; Windows, Enhanced Edition и game load/re-save остаются открытыми gates.

## Read-only local corpus verifier — 2026-09-15

`PYTHON=.venv/bin/python tools/verify_xray_corpus.py` завершился с exit 0;
команда читает локальные файлы и выполняет все правки только в памяти.

| Profile | Candidates | Parsed | Actor version | No-op SHA matches | Edit round-trip operations | Failures |
|---|---:|---:|---:|---:|---:|---:|
| Original Shadow of Chornobyl | 4 | 4 | 118 | 4 | 7 | 0 |
| Original Clear Sky | 56 | 56 | 124 | 56 | 109 | 0 |
| Original Call of Pripyat | 168 | 168 | 128 | 168 | 336 | 0 |

S.T.A.L.K.E.R. 2 и все три Enhanced-профиля дали `0` candidates в обычном
автопоиске этого Linux-хоста; это отсутствие локальных файлов, а не отказ
формата. Личные имена, пути и байты сейвов в evidence не записывались.

Дополнительно `tools/verify_xray_catalog.py` в памяти добавил и повторно
распарсил все official catalog keys: SoC `389/389`, CS `417/417`, CoP
`434/434`, failures `0`. Эта проверка относится к registry framing и
serializer families; игровой load/re-save остаётся отдельным gate.

CLI smoke на одном локальном официальном CoP `.scop` выполнил
`edit --add bandage=2` в новую временную копию: output повторно распознан как
`stalker-cop`, inventory вырос на две base-записи, исходный файл не изменён.
Путь и байты личного сейва в evidence не сохранялись.

## Открытые gates

- Windows artifact/runtime на этом Linux-хосте не собирался и не объявляется
  проверенным.
- Enhanced Edition profiles видны в selector/path discovery, но parser и
  mutations для них отключены: принятых официальных образцов формата нет.
- Для оригинальной трилогии structural catalog-add и deep actor-owned remove
  проверены на принятом контейнере; game load/re-save, equipment/move,
  durability/upgrades, reference-safe удаление и игровые semantics не доказаны.
- Для S.T.A.L.K.E.R. 2 structural add/remove и полный catalog пока не включены:
  подтверждены только деньги и безопасные stack edits.
- Mods намеренно вне scope.
